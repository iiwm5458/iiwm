# NIKKE 截图与战斗数据识别工具交接文档

更新时间：2026-07-01  
项目目录：`C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq`

这份文档用于把当前超长 Codex 对话中的项目状态交接给新的 Codex 对话。新对话开始后，请先阅读本文，再阅读关键源码文件，不要从零重做。

## 1. 项目目标

本项目是一个 Windows 上运行的《胜利女神：新的希望 / NIKKE》C ARENA 自动截图、拼图、战后数据 OCR 识别工具。

核心能力：

1. 自动切换到正在运行的 `nikke.exe` / “胜利女神：新的希望”窗口。
2. 自动点击玩家头像、GROUP 页签、战果按钮、详细战斗数据按钮。
3. 自动截图并拼接：
   - 单人竞技场阵容长图
   - 应援双方阵容图
   - 晋级赛 GROUP 阵容图
   - 64/32/16 强战斗数据图
   - TOP8 冠军争霸赛战斗数据图
   - 赛季全部战斗数据一键采集图
4. 可将截图叠加到底图背景板上输出。
5. OCR 识别战斗数据图像，导出 JSON 与 Excel，用于后续数据分析。
6. GUI 支持深色/月亮主题、粉白主题、截图参数设置、战斗数据图像手动上传识别、妮姬名单手动维护等。

## 2. 运行入口与关键文件

### 2.1 GUI 启动

```powershell
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\run_gui.bat
```

主 GUI 脚本：

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_gui_launcher.ps1
```

### 2.2 截图与拼图核心

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_round_stitcher.py
```

配置文件：

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_round_config.json
```

### 2.3 OCR 工具目录

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\dataanalysis\arena_ocr_tool
```

常见 OCR 相关文件可能在此目录内，包括识别主程序、妮姬名单、特殊名字表、导出逻辑等。下一段对话请先 `rg` 搜索以下关键词定位：

```text
PaddleOCR
nikke_names
special
stage
round
player_id
power
manifest
低内存
中内存
同时导出json
```

### 2.4 输出目录

最终截图、OCR 导出的 JSON/Excel 均应按日期保存到：

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\screenshots\YYYY-MM-DD
```

例如：

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\screenshots\2026-06-29
```

## 3. 环境与运行要求

1. Windows 11。
2. 建议管理员模式运行 GUI，否则自动点击/截图可能无法操作游戏窗口。
3. 游戏需要全屏模式。
4. 若未检测到游戏运行，所有截图按钮都应提示：未检测到正在运行的胜利女神。战斗数据识别（手动上传图片 OCR）不受此限制。
5. OCR 依赖曾使用：

```powershell
python -c "import paddleocr, paddle, rapidfuzz, cv2; print('OCR deps OK')"
```

6. 曾遇到 `numpy` ABI 不兼容，修复方向是让 `opencv-python` / `paddleocr` / `numpy` 版本兼容，不要使用国内镜像，用户明确要求不要用国内镜像资源。

## 4. GUI 当前结构

一级主面板已有或应保留的按钮：

1. `C ARENA单人阵容截图`
2. `应援双方阵容截图`
3. `C ARENA 晋级赛 GROUP阵容截图`
4. `C ARENA TOP8冠军争霸赛`
5. `C ARENA赛季全部战斗数据一键采集`
6. `手动更新妮姬名单`
7. `战斗数据识别`
8. `打开截图文件夹`
9. `截图与数据识别参数设置`

交互要求：

1. 一级页面按钮被激活时高亮；未激活按钮使用普通样式。
2. `打开截图文件夹` 这种工具按钮保持自己的风格，不参与高亮切换。
3. 状态文字：未执行时显示 `空闲`，执行中显示 `正在执行`。
4. 状态下方需要醒目显示 `Alt+2 终止脚本运行`。
5. 用户按 `Alt+2` 成功终止运行时，应弹窗提醒：`任务已紧急停止，指挥官`，并且弹窗需要置顶。
6. 任务完成后提示：`任务已完成，愿命运站在你这一边，指挥官。`
7. 需要管理员权限时弹窗：
   - 标题：`需要指挥官权限`
   - 内容：`请指挥官右键该程序以管理员身份运行`

## 5. 截图功能概览

### 5.1 单人阵容截图

入口：`C ARENA单人阵容截图` 二级页面。

流程：

1. 用户先在游戏中打开竞技场信息页。
2. 点击二级页 `执行截图`。
3. 程序切换到游戏。
4. 点击玩家头像进入玩家资料卡。
5. 截取玩家基础信息。
6. 点击前哨基地页签，截取同步等级与各类研究等级等信息。
7. 回到竞技场信息页，依次截 Round 01 到 Round 05 的阵容卡片。
8. 拼接输出。

重要裁剪要求：

1. 玩家基础信息两边黑边要裁掉。
2. 阵容卡片区域不要包含 Round 页签 UI，不要保留过多边缘留白。
3. 阵容下方黑线不要保留。
4. 部队战斗力/作战人员/时装/部队现状区域要紧贴内容裁剪。
5. 输出还包括前哨基地的八个小等级卡片、同步等级等。

### 5.2 应援双方阵容截图

入口：`应援双方阵容截图` 二级页面。

流程：

1. 用户打开应援信息界面。
2. 点击左侧玩家头像，执行一次单人阵容截图逻辑，但不立即导出。
3. 回到应援页面。
4. 点击右侧玩家头像，执行一次单人阵容截图逻辑，但不立即导出。
5. 将两名玩家阵容按左右两栏拼接。
6. 如果勾选 `应援现状`，额外截取应援现状对比条并拼到最下方。

注意：

1. 应援现状截图两边需要多留一点白边。
2. 二级页面提醒语应为：`请指挥官打开应援信息界面后再执行截图（仅赛前有效）`

### 5.3 C ARENA 晋级赛 GROUP 阵容截图

入口：`C ARENA 晋级赛 GROUP阵容截图`。

二级页面包含：

1. 背景板底图复选按钮。
2. 战后数据区域：
   - `赛后数据（简化）`
   - `赛后数据（详细）`
   - 说明：`当前仅对64强该组8人截图有效` 曾经只适用于 64 强，后续已经扩展到 32/16 强，但文案是否已更新需要检查。
3. `我要所有GROUP的数据`
4. 执行按钮：
   - `执行64强该组8人截图`
   - `执行32强该组4人截图`
   - `执行16强该组双人截图`

#### 5.3.1 64强该组8人截图

点击顺序：

1. 上方左侧 2 人
2. 上方右侧 2 人
3. 下方左侧 2 人
4. 下方右侧 2 人

未勾选战后数据：八人阵容按 2 行 4 列拼接。

勾选 `赛后数据（简化）`：

1. 截完 8 人阵容后，点击 4 个蓝色战果按钮。
2. 每个战果页截取简化胜负区域。
3. 四个简化战果分别插入左上、右上、左下、右下两名玩家中间。

勾选 `赛后数据（详细）`：

1. 点击 4 个蓝色战果按钮。
2. 每个战果页内点击 5 个黑色详细按钮。
3. 每个黑色按钮进入详细页后截图。
4. 每组 5 张详细图从上到下拼接，再分别放入对应两名玩家之间。

勾选 `我要所有GROUP的数据`：

1. 依次点击 GROUP01 到 GROUP08。
2. 每组执行同样逻辑。
3. 最终 GROUP 图片按 2 行 4 列拼接。
4. GROUP 与 GROUP 之间的横向和纵向间距应适当加大，方便用户区分；注意只加大 GROUP 与 GROUP 之间的间距，不改变 GROUP 内阵容间距。

#### 5.3.2 32强该组4人截图

玩家头像顺序：

1. 上方左右两人
2. 下方左右两人

未勾选战后数据：四人阵容单行从左到右排列。

勾选战后数据：

1. 只需要点击 2 个蓝色战果按钮。
2. 进入战果页后需要判断页面底部是否存在 `16强` 与 `32强` 页签。
3. 如果存在，应点击 `32强` 页签，再执行截图或详细按钮逻辑。
4. 如果不存在，不要乱点页签，因为点击空白可能直接关闭页面返回上一层。此时应该直接按 64 强战果页逻辑处理当前页。
5. 简化模式：截取当前战果页所需区域后按一次 Esc 返回。
6. 详细模式：直接依次点击黑色按钮，截图详细页，每次回到战果页，最后回到 GROUP 页面。

已知问题：

1. 过去出现过没有页签时程序多按 Esc，直接返回到更上一层的问题。
2. 执行“所有 GROUP + 32强 + 详细”时曾卡在 GROUP5 最后一名玩家，需要重点检查循环/返回层级/点击坐标。

#### 5.3.3 16强该组双人截图

流程：

1. 点击该组最后 2 名玩家头像，执行单人阵容截图逻辑但不直接导出。
2. 两人阵容左右拼接。
3. 勾选战后数据时：
   - 需要识别两个战果按钮哪个是紫色，哪个是灰色。
   - 只点击紫色按钮，不能点击灰色按钮。
   - 进入战果页后默认是 16 强页，不需要再点页签。
   - 后续简化/详细逻辑套用 32 强战果页逻辑。
4. 勾选 `我要所有GROUP的数据` 时，每组 16 强双人图最终一行横向排列。

### 5.4 C ARENA TOP8 冠军争霸赛

入口：`C ARENA TOP8冠军争霸赛`。

二级页面按钮：

1. `执行8强截图`
2. `执行4强截图`
3. `执行冠亚军截图`
4. `冠军争霸赛战后数据一图流`

注意：不要删除 TOP8 页面里的任何按钮。

#### 5.4.1 执行8强截图

逻辑与 64 强该组 8 人截图一致，但头像位置、4 个战果标签按钮位置不同。

用户已提供过标注图：

```text
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-aaff897e-8be8-4a3f-9c7c-be1b4fe39a18.png
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-59c1857d-0952-4d33-b071-805b02cbe3d0.png
```

#### 5.4.2 执行4强截图

逻辑与 32 强该组 4 人截图一致，但头像位置、2 个战果标签按钮位置不同。

用户已提供过标注图：

```text
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-0a41ae14-f9f7-4501-961e-b1d70a7bbf61.png
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-663748f2-c8a0-46f7-b141-2c5aecc58fa6.png
```

#### 5.4.3 执行冠亚军截图

逻辑与 16 强该组双人截图一致，但头像位置、战果标签按钮位置及颜色识别不同。

要求：

1. 识别两个战果标签页按钮的颜色。
2. 不要点击灰色按钮。
3. 点击粉色按钮。
4. 后续逻辑与 16 强双人截图的战后数据逻辑一致。

用户已提供过标注图：

```text
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-d18e0e3d-347a-4707-8afe-c8d4629dde59.png
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-8ce85804-3b01-4712-9550-c6403c98b5e1.png
```

#### 5.4.4 冠军争霸赛战后数据一图流

按钮说明：`仅在冠军诞生后使用`

逻辑：

1. 依次执行 `执行8强截图`、`执行4强截图`、`执行冠亚军截图`。
2. 三种模式不要分别导出。
3. 按金字塔型排列：
   - `执行8强截图` 结果在最下方，单行排列。
   - `执行4强截图` 结果在第二层，单行排列。
   - `执行冠亚军截图` 结果在最上层。
4. 最后整体导出。

曾修过：一图流最后生成的大面积灰色透明蒙版需要删掉。

### 5.5 赛季全部战斗数据一键采集

入口：`C ARENA赛季全部战斗数据一键采集`。

二级页面当前最新要求：

1. 需要有背景板底图复选按钮。
2. `赛后数据（简化）` 按钮删除或隐藏。
3. `赛后数据（详细）` 默认已勾选，且不可取消选择或关闭。
4. 不需要 `我要所有GROUP的数据`，因为一键采集默认就是全 GROUP + TOP8。
5. 主按钮：`执行一键采集`。

#### 5.5.1 正确的一键采集核心逻辑
“iiwm13本人标注：在之前的对话中已经要求codex在一键采集的截图导出之后不再进行ocr识别战力数据，重心转向到让用户自行选择一键采集导出的4张图片再进行ocr识别”
这部分非常重要。之前实现曾误解成“把所有模式截图重新跑一遍”，这是错误的。

正确逻辑：

1. 对 GROUP01 到 GROUP08，先进入每个 GROUP 的 64 强页面。
2. 每组只在最开始点击 8 名初始玩家头像，采集并缓存：
   - 玩家基础信息截图
   - 阵容截图
   - 玩家 ID
   - 玩家昵称
   - 妮姬阵容 OCR 数据
   - 每名妮姬对应战力
   - 玩家阵容图片路径
3. 后续 32 强、16 强、TOP8 阶段不再点击玩家头像，不再进入玩家资料页，不再重复采集阵容截图。
4. 后续阶段只点击战果标签按钮，进入战果页/详细战果页。
5. 从以下位置 OCR 或交叉验证交战双方玩家昵称：
   - GROUP 比赛出线图中的玩家昵称，注意不要把昵称下方公会名识别进去。
   - 简化战果页中左右双方玩家昵称。
   - 详细战果页中左右双方玩家昵称。
6. 根据识别到的玩家昵称匹配前面 64 人缓存中的阵容截图和数据，进行拼图与 OCR 数据导出。
7. TOP8 玩家必然来自前面 64 人缓存，遇到 TOP8 玩家时不需要也不允许重新补一次单人阵容截图与 OCR。

#### 5.5.2 晋级赛跳转到 TOP8 的路径

用户后来补充的路径：

1. `16强双人截图` 完成后在晋级赛 GROUP 界面。
2. 此时再按一次 Esc，返回到上一级冠军竞技场选择界面。
3. 在该界面右侧有 `冠军争霸赛 / 决赛` 卡片。
4. 点击该卡片进入 TOP8 冠军争霸赛界面。

参考图在临时目录：

```text
C:\Users\iiwm\AppData\Local\Temp\QQ_1782456218055.png
C:\Users\iiwm\AppData\Local\Temp\QQ_1782456264302.png
C:\Users\iiwm\AppData\Local\Temp\QQ_1782456305589.png
C:\Users\iiwm\AppData\Local\Temp\QQ_1782456328554.png
```

#### 5.5.3 已知严重问题

最近完整跑一键采集后存在严重问题：

1. `32进16` 只有 GROUP1 第一组数据，实际应有 8 组数据。
2. 数据中出现不应该出现的玩家昵称，例如 `天然猪肉丸`。“iiwm13本人标注：这里其实是有“天然猪肉丸”这个昵称的玩家的，是我当时没发现有这个玩家”
3. 冠军争霸赛识别数据和拼接图完全错误，交战双方和中间战果数据不对应。“iiwm13本人标注：拼接图错误后来被codex解决了，效果还不错，但是数据识别似乎仍然没有按照我想要的逻辑运行。”
4. 详细战果页截图位置似乎正确，但被拼接到错误玩家阵容之间。“iiwm13本人标注：后面被codex解决了，拼接图目前很完美，如果后面仍然出错我会给codex反馈的。“
5. 例：冠亚军区域中间战果页显示交战双方是 `人世间` 与 `早安抑或晚安`，但拼接的玩家阵容截图却是 `夜莺` 和 `雾雨菇`。
6. 根因大概率是：后续阶段用固定 bracket 位置推断玩家，而不是从战果页/详细页识别玩家昵称后映射缓存，导致人和战果错配。
7. 下一段对话必须优先修复一键采集匹配逻辑，而不是继续堆功能。“iiwm13本人标注：在之前的对话中已经要求codex在截图导出之后不再进行ocr识别战力数据，重心转向到让用户自行选择一键采集导出的4张图片再进行ocr识别”

## 6. OCR 与数据导出

### 6.1 当前 OCR 目标

OCR 需要识别：

1. 攻方选手昵称
2. 攻方选手 ID
3. 守方选手昵称
4. 守方选手 ID
5. 攻方 P1 到 P5 妮姬名称
6. 攻方 P1 到 P5 战力值
7. 守方 P1 到 P5 妮姬名称
8. 守方 P1 到 P5 战力值
9. 每局胜负情况
10. 对局轮次标签

不再识别玩家称号。

### 6.2 Excel 表头要求

当前表头需要使用：

```text
对局轮次
功方选手
功方选手ID
守方选手
守方选手ID
功方P1
功方P1战力
...
功方P5
功方P5战力
守方P1
守方P1战力
...
守方P5
守方P5战力
```

注意：用户明确要求是 `功方`，不是 `攻方`。

### 6.3 对局轮次命名

Excel 第一列和 JSON 对应字段都要按下面格式：

1. 原 `64进32第*组` 改为 `晋级赛第*组8进4`
2. 原 `32进16第*组` 改为 `晋级赛第*组4进2`
3. 原 `16进8第*组` 改为 `晋级赛第*组2进1`
4. `*` 使用阿拉伯数字，不要汉字数字。
5. 原 `M*Round*` 改为 `M*G*`，与前面文字中间留一个空格。

示例：

```text
晋级赛第1组8进4 M1G1
晋级赛第1组4进2 M1G1
晋级赛第1组2进1 G1
冠军争霸赛8进4 M1G1
冠军争霸赛4进2 M1G1
冠军争霸赛冠亚军 G1
```

所有 `2进1` 和 `冠亚军` 对局删除 `M*`，只保留 `G*`。

### 6.4 妮姬名单与特殊名字

已有需求：

1. GUI 有 `手动更新妮姬名单`。
2. 点击后新窗口展示所有妮姬名字和当前名单更新时间。
3. 用户可输入新妮姬名字，一次只能输入一位。
4. 保存后更新名单。
5. 如果用户输入的名字包含中文冒号 `：`，需要自动加入特殊名字库。

特殊名字库非常重要，当前长名字仍容易被误识别成基础角色名：

```text
索达：闪亮兔女郎
普丽瓦蒂：不友善的女仆
安妮：奇迹仙女
阿妮斯：闪耀夏日
索林：霜之旅票
迪塞尔：冬日甜心
拉毗：小红帽
红莲：暗影
布丽德：静默轨道
```

还需要保证角色表包含：

```text
婴宁
画皮
基里
```

用户提出的提高特殊长名识别方案：

1. 把带冒号的名字与不带冒号的名字分离。
2. 识别到包含冒号片段或特殊片段时进入特殊对比。
3. 建立 alias/片段词典，例如：
   - `达：闪亮` -> `索达：闪亮兔女郎`
   - `：闪亮兔` -> `索达：闪亮兔女郎`
   - `闪亮兔女` -> `索达：闪亮兔女郎`
   - `普丽瓦蒂：不友善` -> `普丽瓦蒂：不友善的女仆`
4. 先用特殊库强匹配，再回退到普通妮姬名单 fuzzy match。

### 6.5 战力 OCR 问题

当前战力数值仍有较多错误。

可能原因：

1. 卡片战力裁剪偏移。
2. OCR 对数字误读。
3. 低分辨率/缩放后识别。

可尝试修复：

1. 对每个战力小区域单独裁剪，不要用包含过多背景的区域。
2. 数字识别只允许 `0-9,`，过滤其它字符。
3. 对每个战力区域做多种预处理：
   - 原图
   - 灰度
   - 二值化
   - 放大 2 倍或 3 倍
   - 锐化
4. 多次 OCR 投票，用户提到其它工具可能重复 OCR 10 次。
5. 与详细战果页中的角色/战力信息交叉验证。

### 6.6 OCR 进度窗口

用户要求：

1. 自动化截图流程完成后，如果需要 OCR，不要弹阻塞式 messagebox 等用户点确定。
2. 应直接弹出程序 GUI 中的新窗口。
3. 窗口中显示提醒文字：

```text
战斗数据图像已截取完成，指挥官，正在识别并导出数据，请指挥官耐心等待
```

4. OCR 识别阶段显示明确百分比、已完成/总数、预计剩余时间。
5. 不要只是无限加载等待条。

后续用户又要求：`只做 OCR识别阶段：显示明确百分比 + 已完成/总数 + 预计剩余时间。`

### 6.7 OCR 性能模式

参数设置页名称应为：

```text
截图与数据识别参数设置
```

已调整/需检查：

1. `普通点击后等待自动截图的时间` 滑块默认 0.60 秒。
2. `黑色按钮的详细对战数据加载等待时间` 滑块默认 2.50 秒，但最低值不能变成 2.50，范围仍保持原范围到 5 秒。
3. OCR 性能模式：
   - 默认勾选 `全速CPU`
   - `节能CPU` 与 `均衡CPU` 暂时从 GUI 删除或隐藏，但代码可注释保留。
   - 可有 `极限CPU`，说明必须警告用户。
4. 说明文字应为：

```text
识别64强所有GROUP大图时，请勿使用节能或均衡模式，否则任务可能运行很长时间；建议使用全速CPU。请谨慎使用极限CPU模式，这非常消耗CPU和内存性能，会使风扇转速快速上升、系统响应变慢，若电脑配置不高可能卡死。
```

## 7. 已禁用/暂缓功能

用户后来决定暂时把自动 OCR 导出与低内存/中内存模式收起来，当前重心转到“用户自行上传截图进行识别”。

### 7.1 同时导出 JSON/Excel 复选框

所有 GUI 中的 `同时导出json数据块和excel数据` 复选按钮：

1. 暂时改成灰色。
2. 禁止点击勾选和使用。
3. 鼠标悬浮信息中原 `需同时勾选赛后数据（详细）` 用删除线划掉，后面加：

```text
请指挥官自行选择战斗数据图像进行识别
```

4. 相关功能代码暂时注释掉，不删除。
5. 注释请加清晰标记，建议：

```text
NIKKE_DISABLED_AUTO_OCR_EXPORT_20260630
```

6. 不要影响：
   - 截图逻辑
   - OCR 本身识图逻辑
   - 截图流程中用于判断/匹配的 OCR
   - 一键采集中根据 OCR 进行拼图的逻辑

### 7.2 低内存模式

1. 低内存模式代码暂时全部注释掉，不删除。
2. GUI 中删除低内存模式按钮。
3. 建议注释标记：

```text
NIKKE_DISABLED_LOW_MEMORY_20260630
```

### 7.3 中内存模式

1. 中内存模式按钮暂时灰色，禁止点击勾选或使用。
2. 下方保留一段文字说明，告诉用户该功能暂不可用，后续可能重新启用。
3. 相关代码暂时注释掉，不删除。
4. 建议注释标记：

```text
NIKKE_DISABLED_MEDIUM_MEMORY_20260630
```

## 8. 手动上传战斗数据图像识别页面

一级按钮当前应命名：

```text
战斗数据识别
```

之前曾叫 `上传战斗数据图像进行识别`，用户要求改名为 `战斗数据识别`。

二级页面最新要求：

1. 删除原有 `选择赛后数据截图文件` 按钮/表现形式。
2. 左上角小标题：

```text
选择要识别的赛季全部战斗数据图像
```

3. 标题下方放一个小窗口。
4. 小窗口中有 4 个横向排列的正方形卡槽。
5. 每个卡槽点击后选择一个图像文件。
6. 未选择文件时，卡槽贴图使用用户提供的图片，适当调整透明度和比例：

```text
C:\Users\iiwm\AppData\Local\Temp\codex-clipboard-1a49ec0d-4802-4d94-852a-6e5c583bdc53.png
```

建议复制到项目资产目录：

```text
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\assets\ocr_slot_empty.png
```

7. 已选择图片后，卡槽内显示用户选择图像的缩略图，并且要填充满卡槽。
   - WPF 可用 `Stretch="UniformToFill"` 或裁剪容器实现。
   - 不能像现在那样缩略图没填满。
8. 已选择图片后，卡槽右上角加醒目的 `×`。
9. 点击 `×` 能删除对应卡槽的图片，恢复未选择贴图。
10. 窗口内左下方从上至下展示四段小文字：

```text
TOP8-决赛全部战斗数据（详）
8/16全部战斗数据（详）
16/32全部战斗数据（详）
32/64全部战斗数据（详）
```

11. 每段文字后跟状态：
   - 根据每个卡槽上传图像的文件名判断属于哪一种。
   - 如上传的是 64 强图像，则 `32/64全部战斗数据（详）` 后方显示绿色 `已就绪`。
   - 未满足则灰色 `未就绪`。
12. 删除 `使用例图1测试识别` 按钮。
13. 删除 `打开识别输出文件夹` 按钮。

后续用户会继续说明如何一次性识别四张图，目前只需要选择文件 UI 和状态判断。

## 9. 文件命名规范

用户最后要求按桌面文件规范执行：

```text
C:\Users\iiwm\Desktop\文件名规范.txt
```

下一段对话必须打开此文件，以它为准。

此前用户在对话中给过的命名规则包括但不限于：

### 9.1 单人/应援

```text
单人阵容 + 截图导出日期
应援双方阵容 + 截图导出日期
```

### 9.2 晋级赛 GROUP

未勾选战后数据：

```text
64强该组8人阵容 + 日期
32强该组4人阵容 + 日期
16强该组双人阵容 + 日期
```

仅勾选简化：

```text
64进32当前组8人战斗数据（简） + 日期
32进16当前组4人战斗数据（简） + 日期
16进8当前组双人战斗数据（简） + 日期
```

仅勾选详细：

```text
64进32当前组8人战斗数据（详） + 日期
32进16当前组4人战斗数据（详） + 日期
16进8当前组双人战斗数据（详） + 日期
```

勾选全部 GROUP + 简化：

```text
64进32全部战斗数据（简）
32进16全部战斗数据（简）
16进8全部战斗数据（简）
```

勾选全部 GROUP + 详细：

```text
64进32全部战斗数据（详）
32进16全部战斗数据（详）
16进8全部战斗数据（详）
```

### 9.3 TOP8

未勾选战后数据：

```text
TOP8阵容 + 日期
TOP4阵容 + 日期
决赛阵容 + 日期
```

勾选简化：

```text
8进4全部战斗数据（简） + 日期
4进2全部战斗数据（简） + 日期
决赛战斗数据（简） + 日期
```

勾选详细：

```text
8进4全部战斗数据（详） + 日期
4进2全部战斗数据（详） + 日期
决赛战斗数据（详） + 日期
```

注意：以 `C:\Users\iiwm\Desktop\文件名规范.txt` 为最终准则。

## 10. 背景板底图与自定义目录

多个二级页面都有背景板底图复选按钮：

1. 玛丽安
2. Doro
3. 灰姑娘
4. 自定义

要求：

1. 支持 jpg/png。
2. 用户替换 png 后底图不能消失。
3. 自定义按钮悬浮提示需告诉用户所需图片比例/分辨率与存放目录。
4. 不同页面的自定义目录要区分，不要互相干扰。
5. 背景板底图只裁剪，不统一改透明度。
6. 叠加到背景板上的常规图透明度是 0.86。
7. 透明度只应用于叠加图，不应用于底图。
8. 常规图不要做柔边处理。

## 11. 分辨率适配

项目已经做过自适应分辨率，但 3840x2160 上有特殊问题。

已知：

1. 玩家头像等大部分自动点击在 3840x2160 能正确。
2. `我要所有GROUP的数据` 点击 GROUP 按钮时在 3840x2160 不准。
3. 用户提供了 3840x2160 下 GROUP 按钮标注图：

```text
C:\Users\iiwm\Documents\Tencent Files\...\74a1f9fbff66fb6b67010c5a2daf95c4.png
```

以及临时图在对话里展示过。

建议：

1. GROUP 页签点击不要只按原分辨率比例简单换算。
2. 可以对顶部 GROUP 条区域做相对定位。
3. 或配置不同分辨率下 GROUP 页签的归一化坐标。

## 12. 当前优先级最高的未完成事项

下一段 Codex 对话建议按以下顺序处理：

1. 打开并执行 `C:\Users\iiwm\Desktop\文件名规范.txt` 中的完整命名规则。
2. 修复 `战斗数据识别` 二级页面四个卡槽 UI：
   - 使用用户给的卡槽贴图。
   - 已选图填满卡槽。
   - 右上角 `×` 清除。
   - 状态识别。
3. 修改 `C ARENA赛季全部战斗数据一键采集` 二级页：
   - 删除/隐藏 `赛后数据（简化）`。
   - `赛后数据（详细）` 默认勾选且不可取消。
4. 修复一键采集的核心错误：
   - 后续阶段不再重复点击玩家头像。
   - 用战果页/详细页昵称匹配 64 人缓存。
   - 解决 32进16 只有 GROUP1 的 bug。
   - 修复 TOP8/冠亚军拼错玩家阵容的问题。
5. 强化 OCR：
   - 特殊冒号名字库。
   - 特殊名字片段 alias 映射。
   - 战力数值多策略识别。
   - 日语/韩语昵称输出正确编码。
6. 保证禁用的自动 OCR 导出、低内存/中内存模式不要破坏截图逻辑和内部 OCR 判断。

## 13. 验证命令

PowerShell 语法检查：

```powershell
$errors=$null
[void][System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw -LiteralPath "C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_gui_launcher.ps1"), [ref]$errors)
if($errors){$errors | Format-List; exit 1}else{"ps parse ok"}
```

Python 编译检查：

```powershell
python -m py_compile "C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_round_stitcher.py"
python -m py_compile "C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\dataanalysis\arena_ocr_tool\main.py"
```

OCR 依赖检查：

```powershell
python -c "import paddleocr, paddle, rapidfuzz, cv2; print('OCR deps OK')"
```

搜索关键代码位置：

```powershell
rg -n "战斗数据识别|上传战斗数据|同时导出json|低内存|中内存|赛后数据|TOP8|冠军争霸赛|一键采集|文件名规范|manifest|PaddleOCR|特殊" "C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs"
```

## 14. 设计偏好

用户非常在意 UI 质感：

1. 深色主题要有玻璃质感，不能太土。
2. 粉白主题要淡淡樱花粉白色，不能太深。
3. 面板透明度适中。
4. 按钮要精致，不要粗糙。
5. 复选按钮要小巧精致。
6. 滚动条要细，主题配色，不能有上下按钮，不能超出面板圆角。
7. 面板圆角不要太大。
8. 主面板宽度偏窄一些。
9. 背景图来源链接需要可点击标注。

## 15. 不要做的事

1. 不要删除用户已有的截图、输出数据、素材。
2. 不要使用国内镜像安装依赖。
3. 不要把低内存/中内存/自动 OCR 导出功能彻底删除，只能按要求注释或禁用。
4. 不要破坏截图流程中用于判断按钮颜色、玩家昵称匹配的内部 OCR。
5. 不要在 TOP8 页面删除任何按钮。
6. 不要把 TOP8 玩家当作需要重新补采集的玩家；TOP8 必然来自 64 人缓存。
7. 不要把战果页中公会名误识别成玩家昵称。

## 16. 建议给新 Codex 的开场提示

可以把下面这段直接发给新对话：

```text
请先阅读项目交接文档：
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\PROJECT_HANDOFF_20260701.md

然后检查以下文件：
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_gui_launcher.ps1
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_round_stitcher.py
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\nikke_round_config.json
C:\Users\iiwm\Documents\Codex\2026-06-06\files-mentioned-by-the-user-qq\outputs\dataanalysis\arena_ocr_tool

当前优先任务：
1. 按 C:\Users\iiwm\Desktop\文件名规范.txt 统一输出命名。
2. 修复“战斗数据识别”页面四个文件卡槽 UI。
3. 修改“一键采集”页面：隐藏简化，详细默认勾选不可取消。
4. 修复一键采集的玩家缓存与战果页昵称匹配逻辑，避免 32进16 只出 GROUP1、TOP8 拼错玩家。
```

## 17. 封装建议

封装 exe 前建议完成：

1. 固定依赖版本，尤其 PaddleOCR/Paddle/OpenCV/Numpy。
2. 明确区分用户可写目录：
   - screenshots
   - assets/custom
   - OCR name list
   - config json
3. 第一次运行检查管理员权限。
4. 检查 Win11 缩放比例/DPI。
5. 对 1920x1080、2560x1440、3840x2160 至少各跑一次截图点击测试。
6. 把所有临时路径改为相对 exe 目录或用户数据目录。
7. 封装体积预估：
   - 如果包含 PaddleOCR/Paddle/OpenCV，exe/目录可能非常大，粗略可能 500MB 到 1.5GB 甚至更高。
   - 如果用 one-folder 模式比 one-file 更容易调试和启动。
