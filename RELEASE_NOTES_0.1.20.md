# NIKKE C ARENA Tool 0.1.20 / Lite 0.1.12

发布日期：2026-09-19

## 更新内容

1. 国际服与港澳台服的自动截图改为人工左键确认推进。程序会将鼠标光标定位到每一步的目标位置，等待指挥官亲自左键确认后才继续；国服继续使用原有自动点击逻辑。若点击位置偏离目标，光标会回到目标处，流程不会误推进。
2. 人工确认提示音改为原创霓虹 8bit 音序。提示音包含方波主音与轻微的八度、五度泛音，由后台队列播放，不阻塞截图，也不会出现连续叠音。
3. 完整版的 OCR 标准妮姬名单新增“吉尔提：神力兔女郎”。该条目进入主名单、受保护名单和带冒号特殊名集合，运行时会自动生成安全别名，用于完整名及滚动片段的识别校准。

## 更新原因

- 海外客户端更新后，程序注入式左键点击的兼容性下降。改为用户可见的人工确认，保留截图坐标定位能力并让流程继续可用。
- 原创 8bit 提示音可减少连续操作时的单调感，同时不引入外部音频资产或受版权保护的游戏旋律。
- 新妮姬纳入离线名单，避免 OCR 在新角色出现时将其误识别为基础名或相近角色。

## 补丁范围

- 完整版 `0.1.20` 的跨版本升级补丁适用于所有已发布完整版（`0.1.0` 及以后），更新至当前完整发行内容，并合并新妮姬名单。
- 轻量版 `0.1.12` 的跨版本升级补丁适用于所有已发布轻量版（`0.1.0` 及以后），更新至当前完整发行内容。
- 补丁不会覆盖用户保存的 JSON 参数、主题、赛区选择、截图、OCR 导出、运行日志或自定义背景；被替换的程序文件会备份至安装目录的 `update_backups`。完整版会同步刷新本地名单恢复备份。
