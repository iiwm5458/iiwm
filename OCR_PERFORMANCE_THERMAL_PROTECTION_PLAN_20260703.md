# OCR 性能与温度保护方案

更新时间：2026-07-04

## 背景

四卡槽 OCR 识别过程中，PaddleOCR 会长时间保持较高 CPU 或 GPU 负载。近期一次识别过程中电脑发生硬件级别强制关机，Windows 日志表现为 `Kernel-Power 41`，没有 Python 异常、PaddleOCR 异常或蓝屏转储。结合 OCR 日志可判断：PaddleOCR 很可能是高负载触发因素，但程序层面无法直接证明最终保护动作来自 CPU、GPU、电源或主板热保护。

因此，本方案目标不是单纯提高 OCR 速度，而是让 OCR 默认以更保守、更可控的方式运行，并在用户明确选择时允许性能优先。

## 总体设计

在“截图与数据识别参数设置”的二级页面中新增“性能与温度保护”设置区域。

本方案仅作用于卡槽上传后的战斗数据图像 OCR，也就是手动执行“四卡槽/战斗图像识别”的流程。自动截图、自动采集、截图过程中的窗口识别、点击定位和截图拼接逻辑不接入本方案，不受 block 间歇、控制文件或终止按钮影响。

该设置不替代现有 OCR 运行设备选择，而是与现有 `CPU / GPU` 模式并列：

- OCR 运行设备：`CPU / GPU`
- 性能与温度保护：`过热保护模式 / 性能优先模式`

默认启用 `过热保护模式`。

## 模式说明

### 过热保护模式

推荐模式，默认开启。

程序会主动降低 PaddleOCR 对系统资源的持续占用，优先保证识别过程稳定、可恢复，降低长时间 OCR 触发热保护或硬件保护的概率。

建议策略：

- 不默认限制 PaddleOCR CPU 线程数，避免识别速度过慢。
- 每个对局 block 处理结束后增加短暂 sleep。
- 温度较高时延长 sleep。
- 温度达到暂停阈值时，不再提交下一个 block，等待降温后继续。
- 温度传感器无法读取时，仍启用固定 block 间歇。

### 性能优先模式

用户手动选择后启用。

程序不在中高温阶段主动限速，但仍保留可读取温度下的硬性安全红线。

切换到该模式时必须弹窗确认。

弹窗文案：

```text
指挥官，您正在切换至“性能优先模式”。

该模式将优先保证识别速度，程序不会在中高温阶段主动降速，因此 CPU 或 GPU 可能持续处于高负载状态。长时间运行可能导致设备温度升高、风扇高速运转、性能降频，极端情况下可能触发系统硬件保护并导致自动关机。

为保护您的设备，程序仍会保留 92°C 硬性安全红线：当 CPU 或 GPU 达到警戒温度时，识别任务将被尽快暂停，并建议您终止任务或等待设备降温。

如果温度传感器无法读取，程序无法保证硬性温度暂停生效。

如果您不确定当前散热状态，建议继续使用默认的“过热保护模式”。

是否仍要启用“性能优先模式”？
```

按钮：

- 继续启用性能优先模式
- 返回过热保护模式

## 监控对象

程序需要同时关注 CPU 与 GPU，但主保护对象取决于 OCR 运行设备。

### CPU 模式

- CPU 温度是主保护对象。
- 如果能读取 GPU 温度，也一并显示。
- 如果 GPU 温度达到硬性红线，也暂停任务。

### GPU 模式

- GPU 温度是主保护对象。
- CPU 温度仍需尽量读取和显示。
- 图片预处理、OCR 后处理、Excel/JSON 导出仍可能造成 CPU 高负载。
- 如果 CPU 温度达到硬性红线，也暂停任务。

### 传感器无法读取

如果某个温度传感器无法读取：

- UI 显示“无法读取”。
- 程序不得崩溃。
- 继续显示可读取的 CPU 利用率、内存利用率、GPU 利用率、GPU 显存占用等信息。
- 过热保护模式下启用固定 block 间歇，不限制 PaddleOCR CPU 线程数。
- 性能优先模式下提示用户：无法读取温度时，硬性温度暂停无法保证生效。

## 监控开始与结束

1. 用户点击“开始识别”后，先启动资源与温度监控。
2. 正式 OCR 前做一次启动前温度检查。
3. 如果主保护对象温度已经大于等于 `85°C`，弹窗提醒用户当前设备温度较高，不建议立即开始识别。
4. 用户确认后才继续执行。
5. 任务完成、用户主动终止、任务异常结束时，停止监控并释放资源。
6. 监控线程不得在任务结束后继续运行。

## 正在识别数据窗口

在“正在识别数据”窗口中保留现有进度、日志、耗时等信息，不重复新增已有内容。

新增信息只显示与性能和温度保护直接相关的内容，每 1 秒刷新一次，无需动态图表：

- 当前模式：过热保护模式 / 性能优先模式
- OCR 运行设备：CPU / GPU
- 保护状态：无 / 降速中 / 暂停降温 / 紧急暂停
- CPU 温度：xx°C 或 无法读取
- GPU 温度：xx°C 或 无法读取
- CPU 利用率：xx%
- 内存利用率：xx%
- GPU 利用率：xx% 或 无法读取
- GPU 显存占用：xx MB / xx MB 或 无法读取
- 当前冷却等待时间
- 传感器状态：正常 / 部分无法读取

窗口中增加“终止任务”按钮。

## 温度阈值逻辑

所有温度单位为摄氏度。

### 过热保护模式

正常：

- 主保护对象温度小于 `80°C`。
- 正常识别。

降速：

- 主保护对象温度大于等于 `80°C`。
- 延长每个 block 或小阶段之后的 sleep。
- UI 状态显示“降速中”。

暂停降温：

- 主保护对象温度大于等于 `86°C`。
- 当前小阶段或当前 block 安全结束后，暂停提交新的 OCR 任务。
- UI 状态显示“暂停降温”。

恢复：

- 温度降到 `75-78°C` 以下，并连续稳定 5 秒。
- 自动恢复识别。
- 从未完成的 block 继续，不重复处理已完成 block。

紧急暂停：

- 任意可读取的 CPU 或 GPU 温度大于等于 `92°C`。
- 当前 PaddleOCR 调用无法安全中断时，在当前小阶段结束后尽快暂停。
- 弹窗警告用户温度达到硬性安全红线。
- 建议用户终止任务或等待降温。

说明：

- 如果 GPU 温度读取稳定，可以考虑内部使用更保守的 GPU 紧急阈值，例如 `88-90°C`。
- UI 文案仍可统一显示 `92°C 硬性安全红线`，避免用户理解成本过高。

### 性能优先模式

1. 不主动降速。
2. 不在 `80°C`、`86°C` 阶段限制速度。
3. 任意可读取的 CPU 或 GPU 温度大于等于 `92°C` 时，尽快暂停任务。
4. 弹窗警告用户设备已达到警戒温度，并建议终止任务。
5. 用户可以选择：
   - 终止任务
   - 等待降温后继续
   - 切换为过热保护模式继续

## 采样规则

1. 每 1 秒采样一次。
2. 普通降速、暂停判断参考最近 3 次采样的平均值或最大值。
3. `92°C` 紧急阈值不等待平均值，单次达到即触发。
4. 需要 hysteresis，避免温度在阈值附近反复暂停和恢复。
5. 监控线程必须线程安全，不得阻塞 UI。

## 任务执行方式

当前项目的 OCR 不是多线程 batch 并发，而是按截图、对局 block 顺序处理。

因此，本项目的持续负载控制应落地为：

- 每个 block 或小阶段结束后 sleep。
- 温度过高时暂停提交下一个 block。
- 用户终止时不再提交新的 block。

建议流程：

1. 构建待识别图片和 block 队列。
2. 每次处理 1 个 block 或一个小阶段。
3. 每个 block 结束后检查：
   - 是否用户终止
   - 是否温度过高
   - 是否需要降速
   - 是否需要暂停降温
4. 已完成结果实时写入临时缓存。
5. 用户终止或异常退出时，保留已完成部分结果和日志。

## 终止任务按钮

用户点击“终止任务”后：

1. UI 状态变为“用户终止中”。
2. 停止提交新的识别任务。
3. 尽量等待当前小阶段或当前 block 安全结束。
4. 保存已完成结果。
5. 释放 OCR 模型资源。
6. 停止温度监控。
7. 返回主界面或显示部分结果。
8. 如果 OCR worker 超过 10-15 秒无响应，提示用户是否强制结束识别子进程。

## 实现建议

### GUI 侧

当前 GUI 已经通过 PowerShell 启动 Python OCR 子进程，并通过临时 JSON 轮询 OCR 进度。

建议继续沿用这个结构：

- GUI 主进程负责 UI、温度资源监控、模式切换、终止按钮。
- Python OCR 子进程负责实际识别。
- GUI 通过 `progress.json` 读取 OCR 进度。
- GUI 通过新增 `control.json` 向 OCR 子进程发送控制状态。

`control.json` 可包含：

```json
{
  "mode": "thermal_safe",
  "action": "none",
  "requested_sleep_seconds": 0.3,
  "pause": false,
  "terminate": false,
  "updated_at": "2026-07-03T22:30:00"
}
```

### Python OCR 侧

Python OCR 子进程需要增加：

- `--thermal-mode safe|performance`
- `--control-file path`
- `--cooldown-sleep seconds`

并在每个 block 或小阶段结束后：

- 读取 `control.json`
- 判断是否 sleep
- 判断是否暂停
- 判断是否终止
- 写入临时结果缓存

### PaddleOCR 运行策略

过热保护模式不再默认恢复 CPU 线程限制。

建议策略：

- 保留现有 `--cpu-threads` 参数兼容性，但 GUI 默认不传入。
- `ArenaOCRRecognizer` 默认继续使用 PaddleOCR 后端线程策略。
- 不在过热保护模式下默认设置 `OMP_NUM_THREADS`、`MKL_NUM_THREADS`、`OPENBLAS_NUM_THREADS`、`NUMEXPR_NUM_THREADS`。
- 主要通过 block 间歇、温度暂停和终止控制来降低持续热负载。
- 如果后续需要专家调试入口，可保留隐藏的 `--cpu-threads`，但不作为普通用户默认方案。

当前项目中相关位置：

- `files-mentioned-by-the-user-qq/outputs/dataanalysis/arena_ocr_tool/recognizer/arena_ocr.py`
- `files-mentioned-by-the-user-qq/outputs/dataanalysis/arena_ocr_tool/main.py`
- `files-mentioned-by-the-user-qq/outputs/nikke_gui_launcher.ps1`

### 资源读取

建议使用：

- CPU 利用率、内存利用率：`psutil`
- NVIDIA GPU 温度、利用率、显存：优先 `NVML / nvidia-ml-py`，备用 `nvidia-smi`
- CPU 温度：LibreHardwareMonitorLib 后端

传感器不可用时必须降级显示，不得报错退出。

当前后端选择：

- 采用 `LibreHardwareMonitorLib 0.9.4` 的 `net472` DLL，适配 PowerShell 5.1 / .NET Framework。
- 同目录随包携带 `HidSharp 2.1.0` 依赖。
- vendor 路径：`files-mentioned-by-the-user-qq/outputs/vendor/LibreHardwareMonitorLib/`
- 选择原因：单 DLL 为主、可随产品封装分发、不要求用户额外安装第三方监控软件；GUI 本身已要求管理员运行，有助于提高硬件传感器读取成功率。

## Debug 日志

日志中记录：

- 任务开始时间
- OCR 运行设备
- 温度保护模式
- 温度采样情况
- 降速、暂停、恢复、终止事件
- 最高 CPU 温度
- 最高 GPU 温度
- 已处理 block 数量
- 是否启用固定 block 间歇
- 是否存在温度传感器无法读取

## 分阶段实施建议

### 第一阶段：低风险温度保护

优先实现：

- GUI 增加“过热保护模式 / 性能优先模式”。
- 性能优先模式弹窗确认。
- 默认过热保护模式。
- 每个 block 后增加固定 sleep。
- 进度窗口增加“终止任务”按钮。

这一阶段不依赖温度传感器，工程风险最低，也能在不明显牺牲识别速度的前提下降低 PaddleOCR 长时间持续满载的概率。

### 第二阶段：资源监控 UI

实现：

- CPU 利用率
- 内存利用率
- GPU 利用率
- GPU 显存
- GPU 温度
- 正在识别数据窗口每秒刷新

优先通过 NVML 或 `nvidia-smi` 实现 GPU 读取。

当前实现状态：

- 已在手动卡槽 OCR 的“正在识别数据”窗口中显示 CPU 利用率、内存利用率、GPU 温度、GPU 利用率、GPU 显存占用。
- NVIDIA GPU 信息优先通过本机 `nvidia-smi` 读取。
- CPU 温度暂显示“无法读取”，等待第四阶段接入 LibreHardwareMonitor / OpenHardwareMonitor / HWiNFO 等传感器后端。
- 自动截图、自动采集和截图过程中用于定位/点击/截图的识别逻辑不接入该资源显示与控制链路。

### 第三阶段：温度自动暂停与恢复

实现：

- 启动前温度检查。
- `80°C` 降速。
- `86°C` 暂停降温。
- `75-78°C` 稳定 5 秒后恢复。
- `92°C` 紧急暂停。
- `control.json` 控制 OCR 子进程。

当前实现状态：

- 已接入 `LibreHardwareMonitorLib` 读取 CPU/GPU 温度，NVIDIA GPU 温度仍优先使用 `nvidia-smi`。
- 手动卡槽 OCR 启动前，如果主保护对象温度大于等于 `85°C`，会先弹窗提醒，用户确认后才继续。
- 过热保护模式下：
  - 主保护对象大于等于 `80°C`：将 block 间歇延长到至少 `1.0s`。
  - 主保护对象大于等于 `86°C`：写入控制文件，在当前 block 结束后暂停降温。
  - 主保护对象降到 `78°C` 以下并稳定 5 秒：自动恢复。
  - 任意可读取的 CPU/GPU 温度大于等于 `92°C`：紧急暂停并弹窗提醒。
- 性能优先模式下：
  - 不在 `80°C`、`86°C` 阶段主动降速或暂停。
  - 任意可读取的 CPU/GPU 温度大于等于 `92°C` 时仍会紧急暂停。
- 如果温度传感器无法读取，则显示“无法读取”，不做对应温度阈值动作。
- 该逻辑仍只作用于手动卡槽 OCR，不接入自动截图、自动采集、截图定位或截图拼接流程。

### 第四阶段：CPU 温度后端与部分结果缓存

实现：

- 持续验证 LibreHardwareMonitorLib 在不同机器上的 CPU 温度读取情况。
- 已完成 block 实时写入临时 JSON。
- 用户终止后可导出部分 Excel / JSON。
- 异常退出后可从临时结果辅助排查。

## 关键注意事项

1. 程序无法保证阻止硬件级别瞬时断电。
2. 温度读取失败时，不能假装仍有硬性温度保护。
3. PaddleOCR 单次调用中不建议强行中断。
4. 默认模式必须保守，性能优先必须由用户主动确认。
5. 终止任务应优先安全停止，强制结束子进程只作为最后手段。
6. 该方案应先做第一阶段，再逐步加入温度监控，避免一次性改动过大影响 OCR 主流程稳定性。
